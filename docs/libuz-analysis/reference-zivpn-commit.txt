[33mcommit d64405f5f72595733961f330bd71060df46832fd[m
Author:     kinf744 <adrienyuoi@gmail.com>
AuthorDate: Tue Jun 30 23:38:45 2026 +0100
Commit:     kinf744 <adrienyuoi@gmail.com>
CommitDate: Tue Jun 30 23:38:45 2026 +0100

    feat(zivpn): retire Xray du tunnel UDP — HEV -> LB_PORT -> uz_core direct

[1mdiff --git a/app/src/main/java/com/kighmu/vpn/engines/ZivpnEngine.kt b/app/src/main/java/com/kighmu/vpn/engines/ZivpnEngine.kt[m
[1mindex f786d97..b4369b8 100644[m
[1m--- a/app/src/main/java/com/kighmu/vpn/engines/ZivpnEngine.kt[m
[1m+++ b/app/src/main/java/com/kighmu/vpn/engines/ZivpnEngine.kt[m
[36m@@ -7,7 +7,6 @@[m [mimport com.kighmu.vpn.utils.KighmuLogger[m
 import kotlinx.coroutines.Dispatchers[m
 import kotlinx.coroutines.withContext[m
 import java.io.File[m
[31m-import kotlinx.coroutines.delay[m
 import java.io.InputStream[m
 import java.io.OutputStream[m
 import java.net.ServerSocket[m
[36m@@ -32,171 +31,44 @@[m [mclass ZivpnEngine([m
         }[m
     }[m
 [m
[31m-    // Ports dynamiques par instance pour éviter collisions multi-profil ZIVPN[m
     private val LB_PORT: Int get() = BASE_LB_PORT + (engineIndex * PORT_SPACING)[m
     private val BASE_UZ_PORT: Int get() = BASE_UZ_PORT_START + (engineIndex * PORT_SPACING)[m
 [m
[31m-    private var clashPort: Int = 7890[m
[31m-[m
     @Volatile private var running = false[m
     @Volatile private var uzConnected = false[m
     @Volatile private var serverConnected = false[m
     private var uzProcesses: MutableList<Process> = mutableListOf()[m
     private var lbProcess: Process? = null[m
[31m-    var xrayProcess: Process? = null[m
     private var balancerThread: Thread? = null[m
     private var balancerServerSocket: ServerSocket? = null[m
     private var resolvedServerIp: String = ""[m
     @Volatile private var authErrorCount = 0[m
     @Volatile private var serverErrorCount = 0[m
 [m
[31m-    private fun log(msg: String) {[m
[31m-        KighmuLogger.info(TAG, msg)[m
[31m-    }[m
[32m+[m[32m    private fun log(msg: String) { KighmuLogger.info(TAG, msg) }[m
 [m
     override suspend fun start(): Int {[m
         running = true[m
         serverConnected = false[m
[31m-        clashPort = findFreePort()[m
         return withContext<Int>(Dispatchers.IO) {[m
             val host     = config.zivpnHost.trim()[m
             val password = config.zivpnPassword.trim()[m
[31m-            val port     = config.zivpnPort.trim().ifEmpty { "6000-19999" }[m
[31m-            val obfs     = config.zivpnObfs.trim()[m
[31m-[m
[31m-            log("Démarrage tunnel UDP")[m
[31m-        authErrorCount = 0[m
[31m-        serverErrorCount = 0[m
[31m-            [m
[31m-[m
[32m+[m[32m            log("Demarrage tunnel UDP")[m
[32m+[m[32m            authErrorCount = 0[m
[32m+[m[32m            serverErrorCount = 0[m
             if (host.isEmpty()) throw IllegalArgumentException("Host non configure")[m
             if (password.isEmpty()) throw IllegalArgumentException("Password non configure")[m
[31m-[m
             resolvedServerIp = try {[m
                 java.net.InetAddress.getByName(host).hostAddress ?: host[m
             } catch (_: Exception) { host }[m
[31m-            [m
[31m-[m
[31m-            val clashDir = File(context.filesDir, "clash").also { it.mkdirs() }[m
[31m-            val uzConfigFile = File(clashDir, "uz_config.json")[m
[31m-            uzConfigFile.writeText(buildUzConfig(resolvedServerIp, port, password, obfs))[m
[31m-            [m
[31m-[m
             val nativeDir = context.applicationInfo.nativeLibraryDir[m
[31m-            [m
[31m-[m
[31m-            val libuzCore   = File(nativeDir, "libuz_core.so")[m
[31m-            val libxray     = File(nativeDir, "libxray.so")[m
[31m-[m
[31m-            [m
[31m-            [m
[31m-            [m
[31m-[m
[31m-            if (!libuzCore.exists())[m
[31m-                throw IllegalStateException("Composant UDP introuvable")[m
[31m-            if (!libxray.exists())[m
[31m-                throw IllegalStateException("Composant proxy introuvable")[m
[31m-[m
[31m-            launchXray(libxray, clashDir)[m
[31m-[m
[32m+[m[32m            val libuzCore = File(nativeDir, "libuz_core.so")[m
[32m+[m[32m            if (!libuzCore.exists()) throw IllegalStateException("Composant UDP introuvable")[m
             serverConnected = true[m
[31m-            clashPort[m
[31m-        }[m
[31m-    }[m
[31m-[m
[31m-[m
[31m-[m
[31m-    private fun launchXray(binary: File, clashDir: File) {[m
[31m-        try {[m
[31m-            xrayProcess?.destroyForcibly()[m
[31m-            xrayProcess = null[m
[31m-            // Toujours allouer un nouveau port dynamique pour éviter conflits[m
[31m-            clashPort = findFreePort()[m
[31m-            // Vérification rapide que le port est bien libre[m
[31m-            var waited = 0[m
[31m-            while (waited < 1000) {[m
[31m-                val portFree = try { java.net.ServerSocket(clashPort).also { it.close() }; true } catch (_: Exception) { false }[m
[31m-                if (portFree) break[m
[31m-                clashPort = findFreePort() // Rechoisir si toujours occupé[m
[31m-                Thread.sleep(100)[m
[31m-                waited += 100[m
[31m-            }[m
[31m-            binary.setExecutable(true)[m
[31m-            val nativeDir = context.applicationInfo.nativeLibraryDir[m
[31m-            val xrayConfig = """[m
[31m-{[m
[31m-  "log": {"loglevel": "warning"},[m
[31m-  "inbounds": [{[m
[31m-    "listen": "127.0.0.1",[m
[31m-    "port": $clashPort,[m
[31m-    "protocol": "socks",[m
[31m-    "settings": {"auth": "noauth", "udp": true}[m
[31m-  }],[m
[31m-  "outbounds": [{[m
[31m-    "protocol": "socks",[m
[31m-    "settings": {[m
[31m-      "servers": [{[m
[31m-        "address": "127.0.0.1",[m
[31m-        "port": $LB_PORT[m
[31m-      }][m
[31m-    }[m
[31m-  }][m
[31m-}[m
[31m-""".trimIndent()[m
[31m-            val xrayCfgFile = File(clashDir, "xray_zivpn.json")[m
[31m-            xrayCfgFile.writeText(xrayConfig)[m
[31m-            val pb = ProcessBuilder([m
[31m-                binary.absolutePath,[m
[31m-                "run", "-c", xrayCfgFile.absolutePath[m
[31m-            ).directory(clashDir).apply {[m
[31m-                environment()["LD_LIBRARY_PATH"] = nativeDir[m
[31m-                environment()["HOME"] = clashDir.absolutePath[m
[31m-                environment()["XRAY_LOCATION_ASSET"] = clashDir.absolutePath[m
[31m-                environment()["TMPDIR"] = context.cacheDir.absolutePath[m
[31m-                redirectErrorStream(true)[m
[31m-            }[m
[31m-            xrayProcess = pb.start()[m
[31m-            log("Proxy Secure démarré")[m
[31m-            Thread {[m
[31m-                try {[m
[31m-                    xrayProcess?.inputStream?.bufferedReader()?.forEachLine { line ->[m
[31m-                        if (line.contains("started") || line.contains("listening")) {[m
[31m-                            log("Secure prêt")[m
[31m-                        }[m
[31m-                    }[m
[31m-                    val code = xrayProcess?.waitFor() ?: -1[m
[31m-                    log("Secure arrêté (code=$code)")[m
[31m-                } catch (_: Exception) {}[m
[31m-            }.apply { isDaemon = true }.start()[m
[31m-            // Attente active : Xray prêt dès que le port répond (max 5s, aligné avec XrayVpnEngine)[m
[31m-            var xrayReady = false[m
[31m-            var xrayWaited = 0[m
[31m-            while (xrayWaited < 5000) {[m
[31m-                val alive = try { xrayProcess?.isAlive ?: false } catch (_: Exception) { false }[m
[31m-                if (!alive) break[m
[31m-                val portUp = try {[m
[31m-                    java.net.Socket("127.0.0.1", clashPort).also { it.close() }; true[m
[31m-                } catch (_: Exception) { false }[m
[31m-                if (portUp) { xrayReady = true; break }[m
[31m-                Thread.sleep(50)[m
[31m-                xrayWaited += 50[m
[31m-            }[m
[31m-            val xrayAlive = try { xrayProcess?.isAlive ?: false } catch (_: Exception) { false }[m
[31m-            if (!xrayAlive || !xrayReady) {[m
[31m-                val code = try { xrayProcess?.exitValue() } catch (_: Exception) { -999 }[m
[31m-                log("Erreur Xray (code=$code)")[m
[31m-                throw IllegalStateException("Proxy Xray arrêté au démarrage")[m
[31m-            }[m
[31m-        } catch (e: Exception) {[m
[31m-            log("Erreur Xray: ${e.message}")[m
[31m-            throw e[m
[32m+[m[32m            LB_PORT[m
         }[m
     }[m
 [m
[31m-    private fun buildUzConfig(ip: String, portRange: String, password: String, obfs: String): String {[m
[31m-        return """{"server":"$ip:$portRange","obfs":"$obfs","auth":"$password","socks5":{"listen":"127.0.0.1:7778"},"insecure":true,"recvwindowconn":65536,"recvwindow":262144,"disable_mtu_discovery":true,"down_mbps":50,"up_mbps":10}"""[m
[31m-    }[m
[31m-[m
     private fun buildUzConfigForCore(ip: String, portRange: String, password: String, obfs: String, uzPort: Int): String {[m
         return """{"server":"$ip:$portRange","obfs":"$obfs","auth":"$password","socks5":{"listen":"127.0.0.1:$uzPort"},"insecure":true,"recvwindowconn":65536,"recvwindow":262144,"disable_mtu_discovery":true,"down_mbps":50,"up_mbps":10}"""[m
     }[m
[36m@@ -205,43 +77,33 @@[m [mclass ZivpnEngine([m
         try {[m
             if (vpnService == null) { KighmuLogger.error(TAG, "VpnService non disponible"); return }[m
             launchUzCore()[m
[31m-            val uzPortOk = try { java.net.Socket("127.0.0.1", BASE_UZ_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[31m-            val lbPortOk = try { java.net.Socket("127.0.0.1", LB_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[31m-            log("UZ Core lancé - uz=$uzPortOk lb=$lbPortOk")[m
[31m-        } catch (e: Exception) {[m
[31m-            KighmuLogger.error(TAG, "Erreur launchUzOnly: ${e.message}")[m
[31m-        }[m
[32m+[m[32m            val uzPortOk = try { Socket("127.0.0.1", BASE_UZ_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[32m+[m[32m            val lbPortOk = try { Socket("127.0.0.1", LB_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[32m+[m[32m            log("UZ Core lance - uz=$uzPortOk lb=$lbPortOk")[m
[32m+[m[32m        } catch (e: Exception) { KighmuLogger.error(TAG, "Erreur launchUzOnly: ${e.message}") }[m
     }[m
 [m
     override fun startTun2Socks(fd: Int) {[m
         try {[m
             if (vpnService == null) { KighmuLogger.error(TAG, "VpnService non disponible"); return }[m
             launchUzCore()[m
[31m-            [m
[31m-            val uzPortOk    = try { java.net.Socket("127.0.0.1", BASE_UZ_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[31m-            val lbPortOk    = try { java.net.Socket("127.0.0.1", LB_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[31m-            val clashPortOk = try { java.net.Socket("127.0.0.1", clashPort).also { it.close() }; true } catch (_: Exception) { false }[m
[31m-            [m
[31m-            log("Démarrage interface TUN")[m
[32m+[m[32m            val uzPortOk = try { Socket("127.0.0.1", BASE_UZ_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[32m+[m[32m            val lbPortOk = try { Socket("127.0.0.1", LB_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
[32m+[m[32m            log("Demarrage interface TUN — uz=$uzPortOk lb=$lbPortOk")[m
             HevTun2Socks.init()[m
             if (HevTun2Socks.isAvailable) {[m
                 HevTun2Socks.start(context, fd, LB_PORT, vpnService, mtu = 1400)[m
[31m-                log("Interface TUN active")[m
[32m+[m[32m                log("Interface TUN active -> LB_PORT=$LB_PORT")[m
             } else {[m
                 KighmuLogger.error(TAG, "Interface TUN non disponible")[m
             }[m
[31m-        } catch (e: Exception) {[m
[31m-            KighmuLogger.error(TAG, "Erreur TUN: ${e.message}")[m
[31m-        }[m
[32m+[m[32m        } catch (e: Exception) { KighmuLogger.error(TAG, "Erreur TUN: ${e.message}") }[m
     }[m
 [m
     private fun launchUzCore() {[m
         val portRanges = config.zivpnPort.trim()[m
             .ifEmpty { "6000-19999" }[m
[31m-            .split(",")[m
[31m-            .map { it.trim() }[m
[31m-            .filter { it.isNotEmpty() }[m
[31m-        [m
[32m+[m[32m            .split(",").map { it.trim() }.filter { it.isNotEmpty() }[m
         uzProcesses.forEach { try { it.destroyForcibly() } catch (_: Exception) {} }[m
         uzProcesses.clear()[m
         portRanges.forEachIndexed { index, portRange ->[m
[36m@@ -255,23 +117,13 @@[m [mclass ZivpnEngine([m
             val nativeDir = context.applicationInfo.nativeLibraryDir[m
             val uzBin = File(nativeDir, "libuz_core.so")[m
             val obfs = config.zivpnObfs.trim()[m
[31m-            val cm = context.getSystemService(android.content.Context.CONNECTIVITY_SERVICE)[m
[31m-                    as android.net.ConnectivityManager[m
[32m+[m[32m            val cm = context.getSystemService(android.content.Context.CONNECTIVITY_SERVICE) as android.net.ConnectivityManager[m
             val kighmuService = vpnService as? com.kighmu.vpn.vpn.KighmuVpnService[m
             val physicalNet = kighmuService?.underlyingNetwork ?: cm.activeNetwork[m
[31m-            if (physicalNet != null) {[m
[31m-                cm.bindProcessToNetwork(physicalNet)[m
[31m-                [m
[31m-            } else {[m
[31m-                KighmuLogger.warning(TAG, "Aucun réseau physique disponible")[m
[31m-            }[m
[32m+[m[32m            if (physicalNet != null) cm.bindProcessToNetwork(physicalNet)[m
[32m+[m[32m            else KighmuLogger.warning(TAG, "Aucun reseau physique disponible")[m
             val uzJsonInline = buildUzConfigForCore([m
[31m-                config.zivpnHost.trim(),[m
[31m-                portRange,[m
[31m-                config.zivpnPassword.trim(),[m
[31m-                obfs,[m
[31m-                uzPort[m
[31m-            )[m
[32m+[m[32m                config.zivpnHost.trim(), portRange, config.zivpnPassword.trim(), obfs, uzPort)[m
             val pb = ProcessBuilder(uzBin.absolutePath, "-s", obfs, "--config", uzJsonInline)[m
                 .directory(context.filesDir)[m
                 .apply {[m
[36m@@ -282,77 +134,52 @@[m [mclass ZivpnEngine([m
                 }[m
             val proc = pb.start()[m
             uzProcesses.add(proc)[m
[31m-            [m
             Thread {[m
                 try {[m
                     proc.inputStream.bufferedReader().forEachLine { line ->[m
[31m-                        [m
[31m-                        if (line.contains("connected to server") ||[m
[31m-                            line.contains("ZIVPN UDP running") ||[m
[31m-                            line.contains("SOCKS5 server listening")) {[m
[31m-                            uzConnected = true[m
[31m-                            [m
[31m-                        }[m
[31m-                        if (line.contains("auth") && (line.contains("failed") ||[m
[31m-                            line.contains("error") || line.contains("invalid") ||[m
[31m-                            line.contains("wrong") || line.contains("denied"))) {[m
[32m+[m[32m                        if (line.contains("connected to server") || line.contains("ZIVPN UDP running") ||[m
[32m+[m[32m                            line.contains("SOCKS5 server listening")) { uzConnected = true }[m
[32m+[m[32m                        if (line.contains("auth") && (line.contains("failed") || line.contains("error") ||[m
[32m+[m[32m                            line.contains("invalid") || line.contains("wrong") || line.contains("denied"))) {[m
                             if (authErrorCount < 3) { authErrorCount++; KighmuLogger.error(TAG, "Authentication failed, wrong password") }[m
                         }[m
[31m-                        if (line.contains("connection refused") ||[m
[31m-                            line.contains("connect: no route") ||[m
[31m-                            line.contains("timeout") ||[m
[31m-                            line.contains("unreachable")) {[m
[31m-                            if (serverErrorCount < 3) { serverErrorCount++; KighmuLogger.warning(TAG, "Serveur inaccessible, vérifiez l'adresse et le port") }[m
[32m+[m[32m                        if (line.contains("connection refused") || line.contains("connect: no route") ||[m
[32m+[m[32m                            line.contains("timeout") || line.contains("unreachable")) {[m
[32m+[m[32m                            if (serverErrorCount < 3) { serverErrorCount++; KighmuLogger.warning(TAG, "Serveur inaccessible") }[m
                         }[m
                     }[m
                 } catch (_: Exception) {}[m
                 val code = try { proc.exitValue() } catch (_: Exception) { null }[m
[31m-                if (code != null) KighmuLogger.warning(TAG, "Processus UDP terminé (code=$code)")[m
[32m+[m[32m                if (code != null) KighmuLogger.warning(TAG, "Processus UDP termine (code=$code)")[m
             }.apply { isDaemon = true }.start()[m
[31m-            // Attente active uz_core (max 2s)[m
             var uzWaited = 0[m
             while (uzWaited < 2000) {[m
                 val alive = try { proc.isAlive } catch (_: Exception) { false }[m
                 if (!alive) break[m
[31m-                val portUp = try {[m
[31m-                    java.net.Socket("127.0.0.1", uzPort).also { it.close() }; true[m
[31m-                } catch (_: Exception) { false }[m
[32m+[m[32m                val portUp = try { Socket("127.0.0.1", uzPort).also { it.close() }; true } catch (_: Exception) { false }[m
                 if (portUp) break[m
[31m-                Thread.sleep(50)[m
[31m-                uzWaited += 50[m
[32m+[m[32m                Thread.sleep(50); uzWaited += 50[m
             }[m
             val uzAlive = try { proc.isAlive } catch (_: Exception) { false }[m
             if (!uzAlive) {[m
                 val code = try { proc.exitValue() } catch (_: Exception) { -999 }[m
[31m-                KighmuLogger.warning(TAG, "Canal UDP ${index+1} arrêté (code=$code)")[m
[32m+[m[32m                KighmuLogger.warning(TAG, "Canal UDP ${index+1} arrete (code=$code)")[m
             }[m
[31m-        } catch (e: Exception) {[m
[31m-            KighmuLogger.error(TAG, "Erreur canal UDP: ${e.message}")[m
[31m-        }[m
[32m+[m[32m        } catch (e: Exception) { KighmuLogger.error(TAG, "Erreur canal UDP: ${e.message}") }[m
     }[m
 [m
     private fun launchLbCore() {[m
         try {[m
             Thread.sleep(200)[m
             stopKotlinBalancer()[m
[31m-[m
             val upstreamCount = uzProcesses.size[m
[31m-            if (upstreamCount == 0) {[m
[31m-                KighmuLogger.warning(TAG, "Aucun upstream uz_core disponible")[m
[31m-                return[m
[31m-            }[m
[31m-[m
[31m-            val upstreams = (0 until upstreamCount).map { i ->[m
[31m-                Pair("127.0.0.1", BASE_UZ_PORT + i)[m
[31m-            }[m
[31m-            val counter = java.util.concurrent.atomic.AtomicInteger(0)[m
[31m-            val executor = java.util.concurrent.Executors.newCachedThreadPool()[m
[31m-[m
[31m-            val ss = java.net.ServerSocket(LB_PORT, 128,[m
[31m-                java.net.InetAddress.getByName("127.0.0.1"))[m
[32m+[m[32m            if (upstreamCount == 0) { KighmuLogger.warning(TAG, "Aucun upstream uz_core disponible"); return }[m
[32m+[m[32m            val upstreams = (0 until upstreamCount).map { i -> Pair("127.0.0.1", BASE_UZ_PORT + i) }[m
[32m+[m[32m            val counter = AtomicInteger(0)[m
[32m+[m[32m            val executor = Executors.newCachedThreadPool()[m
[32m+[m[32m            val ss = ServerSocket(LB_PORT, 128, java.net.InetAddress.getByName("127.0.0.1"))[m
             ss.reuseAddress = true[m
             balancerServerSocket = ss[m
[31m-[m
             balancerThread = Thread {[m
                 log("Balanceur Kotlin sur port $LB_PORT (${upstreams.size} upstreams)")[m
                 while (!Thread.currentThread().isInterrupted && !ss.isClosed) {[m
[36m@@ -362,9 +189,8 @@[m [mclass ZivpnEngine([m
                             val idx = counter.getAndIncrement() % upstreams.size[m
                             val (upHost, upPort) = upstreams[idx][m
                             try {[m
[31m-                                val upstream = java.net.Socket(upHost, upPort)[m
[31m-                                upstream.tcpNoDelay = true[m
[31m-                                client.tcpNoDelay = true[m
[32m+[m[32m                                val upstream = Socket(upHost, upPort)[m
[32m+[m[32m                                upstream.tcpNoDelay = true; client.tcpNoDelay = true[m
                                 val t1 = Thread {[m
                                     try { relay(client.inputStream, upstream.outputStream) } catch (_: Exception) {}[m
                                     try { upstream.close() } catch (_: Exception) {}[m
[36m@@ -374,43 +200,28 @@[m [mclass ZivpnEngine([m
                                     try { client.close() } catch (_: Exception) {}[m
                                 }.apply { isDaemon = true }[m
                                 t1.start(); t2.start()[m
[31m-                            } catch (_: Exception) {[m
[31m-                                try { client.close() } catch (_: Exception) {}[m
[31m-                            }[m
[32m+[m[32m                            } catch (_: Exception) { try { client.close() } catch (_: Exception) {} }[m
                         }[m
                     } catch (_: Exception) { break }[m
                 }[m
                 executor.shutdownNow()[m
[31m-                log("Balanceur Kotlin arrêté")[m
[32m+[m[32m                log("Balanceur Kotlin arrete")[m
             }.apply { isDaemon = true; name = "kighmu-lb" }[m
             balancerThread!!.start()[m
[31m-[m
[31m-            // Attente active balanceur (max 1s)[m
[31m-            var lbWaited = 0[m
[31m-            var lbOk = false[m
[32m+[m[32m            var lbWaited = 0; var lbOk = false[m
             while (lbWaited < 1000) {[m
[31m-                lbOk = try {[m
[31m-                    java.net.Socket("127.0.0.1", LB_PORT).also { it.close() }; true[m
[31m-                } catch (_: Exception) { false }[m
[32m+[m[32m                lbOk = try { Socket("127.0.0.1", LB_PORT).also { it.close() }; true } catch (_: Exception) { false }[m
                 if (lbOk) break[m
[31m-                Thread.sleep(30)[m
[31m-                lbWaited += 30[m
[32m+[m[32m                Thread.sleep(30); lbWaited += 30[m
             }[m
[31m-            if (lbOk) log("Balanceur Kotlin opérationnel en ${lbWaited}ms")[m
